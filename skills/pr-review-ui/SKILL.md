---
name: pr-review-ui
description: Interactively generate a side-by-side HTML diff review from chunked PR diff prompts.
version: 1.0.0
triggers:
  - "pr review ui"
  - "generate HTML pr review"
  - "interactive pr review"
  - "next hunk"
  - "next chunk"
intent: code-review
guardrails:
  - Do not use external libraries or CDNs for styling or diff rendering; use pure HTML/CSS.
  - Process strictly one chunk per request to avoid exceeding context limits.
  - Do not output raw markdown diffs when invoked; output the final file URI and confirmation.
tools:
  - bash
  - write_to_file
  - view_file
interface:
  input:
    prompts_dir: "string? — Path to /tmp/pr-review/<randomid>/prompts/"
  output:
    html_file: "string — Path to generated /tmp/pr-review/<randomid>/review_p<N>.html"
---

## What this skill does

The `pr-review-ui` skill acts as an automated interactive assistant that takes chunked prompt diffs (generated by the `pr-review` skill) and performs a deep code review on each chunk via the agent's LLM capabilities. It then renders a dependency-free, styled HTML file containing a side-by-side (left/right) view of the diff with inline review comments injected.

## Agent Instructions

When the user triggers this skill:

1. **Locate Prompts**: If the user didn't specify a directory, look for recently modified `/tmp/pr-review/*/prompts/` directories. If there are none, invite them to run `pr-review` first.
2. **Determine Chunk State**: Identify which snippet needs to be reviewed. If the user just started, pick `p1.txt`. If they said "next hunk/chunk", inspect the directory to find the sequentially next file (e.g. `p2.txt`, `p3.txt`) that hasn't been reviewed yet (has no matching `review_p*.html`).
3. **Read and Review**: Read the selected `.txt` file using the `view_file` tool. Act as a senior engineer as instructed in the prompt's `Review Instructions`. Formulate inline review comments inside your internal model context.
4. **Render HTML**: Use the *Pure HTML/CSS Diff Template* below to construct the UI layout. Generate standard `<tr>` elements representing the side-by-side diff.
   - **Old Code (Left)**: Show removed lines (`-`) or unchanged lines. Empty for added lines (`+`).
   - **New Code (Right)**: Show added lines (`+`) or unchanged lines. Empty for removed lines (`-`).
   - **Inline comments (LLM feedback)**: If a specific line or block warrants a comment from your code review logic, inject a spanning row `<tr><td colspan="4" class="inline-comment">YOUR COMMENT HTML</td></tr>` immediately beneath the relevant diff row.
5. **Save and Present**: Write the generated HTML to the parent `/tmp/pr-review/<randomid>/review_p<N>.html` using `write_to_file`. Present the absolute file URI starting with `file:///tmp/...` to the user as a clickable link. Prompt the user: `"Review chunk <N> generated. Say 'next hunk' to process the next set of files."`

---

## Pure HTML/CSS Diff Template

Agent, when writing the HTML file, tightly adhere to the following template structure:

```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<style>
  :root {
    --bg: #0d1117; --text: #c9d1d9; --border: #30363d;
    --add-bg: #113324; --add-text: #2ea043;
    --del-bg: #33151b; --del-text: #f85149;
    --comment-bg: #1f2428; --comment-border: #58a6ff;
  }
  body {
    background: var(--bg); color: var(--text);
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
    margin: 0; padding: 20px;
  }
  .diff-table {
    width: 100%; border-collapse: collapse; font-family: ui-monospace, SFMono-Regular, "SF Mono", Menlo, Consolas, "Liberation Mono", monospace;
    font-size: 12px; line-height: 1.5; border: 1px solid var(--border);
  }
  .diff-table th { background: #161b22; color: #8b949e; text-align: left; padding: 4px 10px; border-bottom: 1px solid var(--border); border-right: 1px solid var(--border); }
  .diff-table td { padding: 2px 10px; white-space: pre-wrap; word-break: break-all; border-right: 1px solid var(--border); }
  .line-num { color: #6e7681; width: 40px; text-align: right; user-select: none; border-right: 1px solid var(--border); }
  
  .del-row td { background: var(--del-bg); }
  .add-row td { background: var(--add-bg); }
  .del-line { color: var(--del-text); }
  .add-line { color: var(--add-text); }
  
  .inline-comment {
    background: var(--comment-bg); border-left: 4px solid var(--comment-border);
    padding: 10px 15px !important; font-family: inherit; font-size: 14px;
  }
  .header { margin-bottom: 20px; }
  h1, h2, h3 { color: #fff; }
</style>
</head>
<body>
  <div class="header">
    <h1>PR Review: Chunk N</h1>
    <p>Context and Description Header goes here (parsed from prompt).</p>
  </div>
  <table class="diff-table">
    <thead>
      <tr>
        <th colspan="2">Old (Left)</th>
        <th colspan="2">New (Right)</th>
      </tr>
    </thead>
    <tbody>
      <!-- DIFF ROWS GO HERE. Example: -->
      <!-- 
      <tr class="del-row">
        <td class="line-num">42</td><td>- old removed code</td>
        <td class="line-num"></td><td></td>
      </tr>
      <tr class="add-row">
        <td class="line-num"></td><td></td>
        <td class="line-num">42</td><td>+ new added code</td>
      </tr>
      <tr>
        <td colspan="4" class="inline-comment"><strong>Agent Comment:</strong> This change introduces a resource leak! Wait...</td>
      </tr>
      -->
    </tbody>
  </table>
</body>
</html>
```
